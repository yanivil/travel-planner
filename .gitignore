# dependencies / build
node_modules/
dist/
dev-dist/
.env
.env.*
!.env.example

# test output
coverage/
playwright-report/
test-results/

# OS noise
.DS_Store

# local personal fixtures — real trip data never enters this public repo (see CLAUDE.md)
local/

# spike artifacts — never commit heavy/generated spike files
spikes/**/node_modules/
spikes/**/dist/
*.pmtiles
